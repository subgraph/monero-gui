#!/usr/bin/make -f

%:
	dh "$@"

override_dh_auto_configure:
	cd $(CURDIR) && echo "var GUI_VERSION = \"v0.11.1.0\"\nvar GUI_MONERO_VERSION = \"v0.11.0.0\"" >version.js
	qmake
	make -C src/zxcvbn-c
	
override_dh_auto_install:
	dh_auto_install -- prefix=/usr
